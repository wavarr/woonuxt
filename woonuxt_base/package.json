{
  "name": "woonuxt",
  "version": "4.4.3",
  "main": "./nuxt.config.ts",
  "type": "module",
  "dependencies": {
    "nuxi": "^3.22.2",
    "nuxt": "^3.15.4"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "mkdir -p .vercel/output/static && cp -r public/* .vercel/output/static/",
    "generate": "nuxt generate --standalone",
    "preview": "nuxt preview"
  }
}
